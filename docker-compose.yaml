version: '3.4'

services:
  frontend:
    build:
      context: ./frontend/aboutpets-app
      dockerfile: Dockerfile
    ports:
      - "3000:80"

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      SA_PASSWORD: SaraAttila1994
      ACCEPT_EULA: Y
    ports:
      - "1433:1433"
    platform: linux/amd64
