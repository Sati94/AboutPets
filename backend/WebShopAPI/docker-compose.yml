version: '3.4'

services:
  webshopapi:
    image: ${DOCKER_REGISTRY-}aboutpets
    build:
      context: .
      dockerfile: WebShopAPI/Dockerfile
    ports:
      - "8080:80"
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=PetProject;User Id=sa;Password=SaraAttila1994;Encrypt=False;TrustServerCertificate=True;MultipleActiveResultSets=True
    depends_on:
      - sqlserver
    platform: linux/amd64

  sqlserver:
    image:  mcr.microsoft.com/mssql/server:2019-latest
    environment:
      SA_PASSWORD: SaraAttila1994
      ACCEPT_EULA: Y
    ports:
      - "1433:1433"
    platform: linux/amd64